[package]
name = "rms-check"
version = "0.1.0"
authors = ["Renée Kooi <renee@kooi.me>"]
license = "GPL-3.0"
description = "Detect common problems with AoE Random Map Scripts"
repository = "https://github.com/goto-bus-stop/rms-check.git"
edition = "2018"

[dependencies]
codespan = "^0.5.0"
codespan-reporting = "^0.5.0"
itertools = "^0.8.1"
lazy_static = "^1.4.0"
strsim = "^0.9.2"
zip = { version = "0.5.3", default-features = false } # only need `store`
